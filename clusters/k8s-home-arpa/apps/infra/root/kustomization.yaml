apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # sync-wave -2: namespaces first
  - ../namespaces
  # sync-wave -1: projects before apps
  - ../projects
  # sync-wave 0: ArgoCD config
  - ../config
  # sync-wave 1: platform Applications (includes cilium-lb, gateway-config at sync-wave 2)
  - ../applications
