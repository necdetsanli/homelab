# HTTPRoute: zabbix.mgmt.home.arpa -> Zabbix web UI in monitoring
# HAProxy terminates external TLS (Vault ACME cert) and re-encrypts to the
# Gateway's HTTPS listener (Vault PKI cert via cert-manager). This route
# matches by hostname and forwards to the Zabbix web service.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: zabbix-web
  namespace: monitoring
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: platform-gateway
      namespace: gateway-system
      sectionName: https
  hostnames:
    - "zabbix.mgmt.home.arpa"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: zabbix-zabbix-web
          port: 80
          weight: 1
