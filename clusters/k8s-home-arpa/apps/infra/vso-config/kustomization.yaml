apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # vault-ca-bundle.yaml removed — CA bundle now managed by vault-ca-bundle Application
  # (single source of truth: vault-ca-bundle/root-ca.crt → security namespace)
  - vault-connection.yaml
  - vault-auth.yaml
  - vault-static-secret-test.yaml
