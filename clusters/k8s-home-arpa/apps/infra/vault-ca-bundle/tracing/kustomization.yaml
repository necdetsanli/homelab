# Vault CA Bundle → tracing namespace
# Required by Jaeger to verify Elasticsearch TLS certificate (Vault PKI issued).
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tracing
resources:
  - ../base
