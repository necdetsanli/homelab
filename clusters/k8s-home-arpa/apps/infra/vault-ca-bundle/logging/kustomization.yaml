# Vault CA Bundle → logging namespace
# Required by Elasticsearch cert-manager Certificate (trusting Vault PKI chain)
# and any clients that need to verify Vault-issued TLS certs.
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: logging
resources:
  - ../base
