# Vault CA Bundle → monitoring namespace
# Required by Grafana to verify Elasticsearch TLS certificate (Vault PKI issued).
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
resources:
  - ../base
