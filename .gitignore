# ==============================================================================
# Homelab GitOps .gitignore — security-first, format-agnostic
#
# Principle: ignore SECRETS and STATE, not file formats.
# Config files (.cfg, .conf, .yaml) are safe to commit as long as they
# contain no credentials. Secrets belong in Vault, not in git.
# ==============================================================================

# ------------------------------------------------------------------------------
# Local-only files
# ------------------------------------------------------------------------------
PLAN.md

# ------------------------------------------------------------------------------
# Secrets & credentials
# ------------------------------------------------------------------------------
# Environment files (may contain tokens/passwords)
.env
.env.*
!.env.example
!.env.template

# Secret directories
secrets/
secret/
private/

# Private keys & certificates (key material)
*.key
*.pem
*.pfx
*.p12
*.jks
*.keystore
*.crt
*.csr
*.der

# Encryption keys
*.age
*.agekey
*.gpg
*.pgp
*.asc

# VPN / device profiles
*.ovpn
*.mobileconfig

# Vault tokens
.vault-token

# ------------------------------------------------------------------------------
# Kubernetes sensitive files
# ------------------------------------------------------------------------------
*.kubeconfig
kubeconfig
**/kubeconfig
**/kubeconfig.*

# Helm secrets plugin
secrets.yaml
secrets.*.yaml
!secrets.example.yaml

# ------------------------------------------------------------------------------
# SOPS config (safe to commit — these are pointers, not keys)
# ------------------------------------------------------------------------------
!.sops.yaml
!.sops.yml

# ------------------------------------------------------------------------------
# Runtime state & logs
# ------------------------------------------------------------------------------
*.log
*.log.*
*.out
*.err
*.pid
*.sock
*.tmp
*.swp
*.swo

logs/
log/
data/
state/
run/
tmp/
temp/
.cache/
cache/

# Databases / persistence
*.db
*.sqlite
*.sqlite3
*.rdb
dump.rdb
appendonly.aof

# ------------------------------------------------------------------------------
# Terraform / OpenTofu
# ------------------------------------------------------------------------------
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
*.tfvars
*.tfvars.json
!*.tfvars.example

# Terragrunt
.terragrunt-cache/

# Pulumi
.pulumi/

# ------------------------------------------------------------------------------
# Ansible
# ------------------------------------------------------------------------------
*.retry

# Downloaded vendor collections (not your code)
ansible/collections/ansible_collections/

# Inventory with real hosts — commit templates only
ansible/inventory/*
!ansible/inventory/*.example

# ------------------------------------------------------------------------------
# Kubernetes / Helm build artifacts
# ------------------------------------------------------------------------------
charts/**/charts/
charts/**/tmp/
helmfile*.lock
*.tgz

# ------------------------------------------------------------------------------
# Container / build artifacts
# ------------------------------------------------------------------------------
docker-compose.override.yml
docker-compose.override.yaml
**/dist/
**/build/
**/target/
**/*.tar
**/*.tar.gz

# ------------------------------------------------------------------------------
# CI / scanner output
# ------------------------------------------------------------------------------
coverage/
.nyc_output/
reports/
report/
*.sarif
*.sbom
*.lcov

# ------------------------------------------------------------------------------
# OS & editor
# ------------------------------------------------------------------------------
.DS_Store
Thumbs.db
Desktop.ini

.vscode/
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json

.idea/
*.iml

# ------------------------------------------------------------------------------
# Language tooling (DevOps scripting)
# ------------------------------------------------------------------------------
node_modules/
__pycache__/
*.py[cod]
.venv/
venv/

# Go
bin/
pkg/
*.exe
*.test
*.prof

# ------------------------------------------------------------------------------
# SSH (never commit)
# ------------------------------------------------------------------------------
**/.ssh/
known_hosts
authorized_keys

# ------------------------------------------------------------------------------
# Backups
# ------------------------------------------------------------------------------
*.bak
*.backup
*.old
*.orig
*.save